services:
  - type: web
    name: Kieran-Christien
    region: frankfurt
    plan: free
    runtime: python
    rootDir: web
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app -w 2 -b 0.0.0.0:$PORT
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: PORT
        value: 5000
      - key: SMTP_ADDRESS
        value: smtp.gmail.com
      - key: SMTP_PORT
        value: 587
      - key: EMAIL_ADDRESS
        value: kieranchristien@gmail.com
      - key: EMAIL_PASSWORD
        value: budp fnyk zjjw qchl